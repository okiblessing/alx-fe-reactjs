// src/components/UserInfo.jsx

import UserDetails from './UserDetails';

function UserInfo() {
  return <UserDetails />;  {/* No need to pass userData as a prop anymore */}
}

export default UserInfo;

