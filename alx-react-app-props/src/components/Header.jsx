import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>My Favorite Cities</h1>
    </header>
  );
};

// Make sure to export the Header component
export default Header;
